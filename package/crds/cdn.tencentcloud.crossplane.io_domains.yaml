---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: domains.cdn.tencentcloud.crossplane.io
spec:
  group: cdn.tencentcloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - tencentcloudjet
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Domain is the Schema for the Domains API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of Domain
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  area:
                    description: 'Domain name acceleration region. `mainland`: acceleration
                      inside mainland China, `overseas`: acceleration outside mainland
                      China, `global`: global acceleration. Overseas acceleration
                      service must be enabled to use overseas acceleration and global
                      acceleration.'
                    type: string
                  authentication:
                    description: 'Specify timestamp hotlink protection configuration,
                      NOTE: only one type can choose for the sub elements.'
                    items:
                      properties:
                        switch:
                          description: 'Authentication switching, available values:
                            `on`, `off`.'
                          type: string
                        typeA:
                          description: Timestamp hotlink protection mode A configuration.
                          items:
                            properties:
                              backupSecretKey:
                                description: Used for calculate a signature. 6-32
                                  characters. Only digits and letters are allowed.
                                type: string
                              expireTime:
                                description: Signature expiration time in second.
                                  The maximum value is 630720000.
                                type: number
                              fileExtensions:
                                description: 'File extension list settings determining
                                  if authentication should be performed. NOTE: If
                                  it contains an asterisk (*), this indicates all
                                  files.'
                                items:
                                  type: string
                                type: array
                              filterType:
                                description: 'Available values: `whitelist` - all
                                  types apart from `file_extensions` are authenticated,
                                  `blacklist`: - only the types in the `file_extensions`
                                  are authenticated.'
                                type: string
                              secretKey:
                                description: 'The key for signature calculation. Only
                                  digits, upper and lower-case letters are allowed.
                                  Length limit: 6-32 characters.'
                                type: string
                              signParam:
                                description: 'Signature parameter name. Only upper
                                  and lower-case letters, digits, and underscores
                                  (_) are allowed. It cannot start with a digit. Length
                                  limit: 1-100 characters.'
                                type: string
                            required:
                            - expireTime
                            - fileExtensions
                            - filterType
                            - secretKey
                            - signParam
                            type: object
                          type: array
                        typeB:
                          description: 'Timestamp hotlink protection mode B configuration.
                            NOTE: according to upgrading of TencentCloud Platform,
                            TypeB is unavailable for now.'
                          items:
                            properties:
                              backupSecretKey:
                                description: Used for calculate a signature. 6-32
                                  characters. Only digits and letters are allowed.
                                type: string
                              expireTime:
                                description: Signature expiration time in second.
                                  The maximum value is 630720000.
                                type: number
                              fileExtensions:
                                description: 'File extension list settings determining
                                  if authentication should be performed. NOTE: If
                                  it contains an asterisk (*), this indicates all
                                  files.'
                                items:
                                  type: string
                                type: array
                              filterType:
                                description: 'Available values: `whitelist` - all
                                  types apart from `file_extensions` are authenticated,
                                  `blacklist`: - only the types in the `file_extensions`
                                  are authenticated.'
                                type: string
                              secretKey:
                                description: 'The key for signature calculation. Only
                                  digits, upper and lower-case letters are allowed.
                                  Length limit: 6-32 characters.'
                                type: string
                            required:
                            - expireTime
                            - fileExtensions
                            - filterType
                            - secretKey
                            type: object
                          type: array
                        typeC:
                          description: Timestamp hotlink protection mode C configuration.
                          items:
                            properties:
                              backupSecretKey:
                                description: Used for calculate a signature. 6-32
                                  characters. Only digits and letters are allowed.
                                type: string
                              expireTime:
                                description: Signature expiration time in second.
                                  The maximum value is 630720000.
                                type: number
                              fileExtensions:
                                description: 'File extension list settings determining
                                  if authentication should be performed. NOTE: If
                                  it contains an asterisk (*), this indicates all
                                  files.'
                                items:
                                  type: string
                                type: array
                              filterType:
                                description: 'Available values: `whitelist` - all
                                  types apart from `file_extensions` are authenticated,
                                  `blacklist`: - only the types in the `file_extensions`
                                  are authenticated.'
                                type: string
                              secretKey:
                                description: 'The key for signature calculation. Only
                                  digits, upper and lower-case letters are allowed.
                                  Length limit: 6-32 characters.'
                                type: string
                              timeFormat:
                                description: 'Timestamp formation, available values:
                                  `dec`, `hex`.'
                                type: string
                            required:
                            - expireTime
                            - fileExtensions
                            - filterType
                            - secretKey
                            type: object
                          type: array
                        typeD:
                          description: Timestamp hotlink protection mode D configuration.
                          items:
                            properties:
                              backupSecretKey:
                                description: Used for calculate a signature. 6-32
                                  characters. Only digits and letters are allowed.
                                type: string
                              expireTime:
                                description: Signature expiration time in second.
                                  The maximum value is 630720000.
                                type: number
                              fileExtensions:
                                description: 'File extension list settings determining
                                  if authentication should be performed. NOTE: If
                                  it contains an asterisk (*), this indicates all
                                  files.'
                                items:
                                  type: string
                                type: array
                              filterType:
                                description: 'Available values: `whitelist` - all
                                  types apart from `file_extensions` are authenticated,
                                  `blacklist`: - only the types in the `file_extensions`
                                  are authenticated.'
                                type: string
                              secretKey:
                                description: 'The key for signature calculation. Only
                                  digits, upper and lower-case letters are allowed.
                                  Length limit: 6-32 characters.'
                                type: string
                              timeFormat:
                                description: 'Timestamp formation, available values:
                                  `dec`, `hex`.'
                                type: string
                              timeParam:
                                description: 'Timestamp parameter name. Only upper
                                  and lower-case letters, digits, and underscores
                                  (_) are allowed. It cannot start with a digit. Length
                                  limit: 1-100 characters.'
                                type: string
                            required:
                            - expireTime
                            - fileExtensions
                            - filterType
                            - secretKey
                            type: object
                          type: array
                      type: object
                    type: array
                  awsPrivateAccess:
                    description: Access authentication for S3 origin.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: Access ID.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        bucket:
                          description: Bucket.
                          type: string
                        region:
                          description: Region.
                          type: string
                        secretKeySecretRef:
                          description: Key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  bandWidthAlert:
                    description: Bandwidth cap configuration.
                    items:
                      properties:
                        alertPercentage:
                          description: Alert percentage.
                          type: number
                        alertSwitch:
                          description: Switch alert.
                          type: string
                        bpsThreshold:
                          description: threshold of bps.
                          type: number
                        counterMeasure:
                          description: Counter measure.
                          type: string
                        metric:
                          description: Metric.
                          type: string
                        statisticItem:
                          description: Specify statistic item configuration.
                          items:
                            properties:
                              alertPercentage:
                                description: Alert percentage.
                                type: number
                              alertSwitch:
                                description: Switch alert.
                                type: string
                              bpsThreshold:
                                description: threshold of bps.
                                type: number
                              counterMeasure:
                                description: 'Counter measure, values: `RETURN_404`,
                                  `RESOLVE_DNS_TO_ORIGIN`.'
                                type: string
                              cycle:
                                description: Cycle of checking in minutes, values
                                  `60`, `1440`.
                                type: number
                              metric:
                                description: Metric.
                                type: string
                              switch:
                                description: 'Configuration switch, available values:
                                  `on`, `off` (default).'
                                type: string
                              type:
                                description: Type of statistic item.
                                type: string
                              unblockTime:
                                description: Time of auto unblock.
                                type: number
                            required:
                            - switch
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  compression:
                    description: Smart compression configurations.
                    items:
                      properties:
                        compressionRules:
                          description: List of compression rules.
                          items:
                            properties:
                              algorithms:
                                description: 'List of algorithms, available: `gzip`
                                  and `brotli`.'
                                items:
                                  type: string
                                type: array
                              compress:
                                description: Must be set as true, enables compression.
                                type: boolean
                              fileExtensions:
                                description: List of file extensions like `jpg`, `txt`.
                                items:
                                  type: string
                                type: array
                              maxLength:
                                description: The maximum file size to trigger compression
                                  (in bytes).
                                type: number
                              minLength:
                                description: The minimum file size to trigger compression
                                  (in bytes).
                                type: number
                              rulePaths:
                                description: 'List of rule paths for each `rule_type`:
                                  `*` for `all`, file ext like `jpg` for `file`, `/dir/like/`
                                  for `directory` and `/path/index.html` for `path`.'
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: 'Rule type, available: `all`, `file`,
                                  `directory`, `path`, `contentType`.'
                                type: string
                            required:
                            - algorithms
                            - compress
                            - maxLength
                            - minLength
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  domain:
                    description: Name of the acceleration domain.
                    type: string
                  downstreamCapping:
                    description: Downstream capping configuration.
                    items:
                      properties:
                        cappingRules:
                          description: List of capping rule.
                          items:
                            properties:
                              kbpsThreshold:
                                description: Capping rule kbps threshold.
                                type: number
                              rulePaths:
                                description: List of capping rule path.
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: Capping rule type.
                                type: string
                            required:
                            - kbpsThreshold
                            - rulePaths
                            - ruleType
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  errorPage:
                    description: Error page configurations.
                    items:
                      properties:
                        pageRules:
                          description: List of error page rule.
                          items:
                            properties:
                              redirectCode:
                                description: Redirect code of error page rules.
                                type: number
                              redirectUrl:
                                description: Redirect url of error page rules.
                                type: string
                              statusCode:
                                description: Status code of error page rules.
                                type: number
                            required:
                            - redirectCode
                            - redirectUrl
                            - statusCode
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  explicitUsingDryRun:
                    description: 'Used for validate only by store arguments to request
                      json string as expected, WARNING: if set to `true`, NO Cloud
                      Api will be invoked but store as local data, do not use this
                      argument unless you really know what you are doing.'
                    type: boolean
                  followRedirectSwitch:
                    description: '301/302 redirect following switch, available values:
                      `on`, `off` (default).'
                    type: string
                  fullUrlCache:
                    description: Whether to enable full-path cache. Default value
                      is `true`.
                    type: boolean
                  httpsConfig:
                    description: HTTPS acceleration configuration. It's a list and
                      consist of at most one item.
                    items:
                      properties:
                        clientCertificateConfig:
                          description: Client certificate configuration information.
                          items:
                            properties:
                              certificateContent:
                                description: Client Certificate PEM format, requires
                                  Base64 encoding.
                                type: string
                            required:
                            - certificateContent
                            type: object
                          type: array
                        forceRedirect:
                          description: Configuration of forced HTTP or HTTPS redirects.
                          items:
                            properties:
                              redirectStatusCode:
                                description: Forced redirect status code. Valid values
                                  are `301` and `302`. When `switch` setting `off`,
                                  this property does not need to be set or set to
                                  `302`. Default value is `302`.
                                type: number
                              redirectType:
                                description: Forced redirect type. Valid values are
                                  `http` and `https`. `http` means a forced redirect
                                  from HTTPS to HTTP, `https` means a forced redirect
                                  from HTTP to HTTPS. When `switch` setting `off`,
                                  this property does not need to be set or set to
                                  `http`. Default value is `http`.
                                type: string
                              switch:
                                description: Forced redirect configuration switch.
                                  Valid values are `on` and `off`. Default value is
                                  `off`.
                                type: string
                            type: object
                          type: array
                        http2Switch:
                          description: HTTP2 configuration switch. Valid values are
                            `on` and `off`. and default value is `off`.
                          type: string
                        httpsSwitch:
                          description: HTTPS configuration switch. Valid values are
                            `on` and `off`.
                          type: string
                        ocspStaplingSwitch:
                          description: OCSP configuration switch. Valid values are
                            `on` and `off`. and default value is `off`.
                          type: string
                        serverCertificateConfig:
                          description: Server certificate configuration information.
                          items:
                            properties:
                              certificateContent:
                                description: Server certificate information. This
                                  is required when uploading an external certificate,
                                  which should contain the complete certificate chain.
                                type: string
                              certificateId:
                                description: Server certificate ID.
                                type: string
                              message:
                                description: Certificate remarks.
                                type: string
                              privateKey:
                                description: Server key information. This is required
                                  when uploading an external certificate.
                                type: string
                            type: object
                          type: array
                        spdySwitch:
                          description: Spdy configuration switch. Valid values are
                            `on` and `off`. and default value is `off`. This parameter
                            is for white-list customer.
                          type: string
                        tlsVersions:
                          description: Tls version settings, only support some Advanced
                            domain names, support settings TLSv1, TLSV1.1, TLSV1.2,
                            TLSv1.3, when modifying must open consecutive versions.
                          items:
                            type: string
                          type: array
                        verifyClient:
                          description: Client certificate authentication feature.
                            Valid values are `on` and `off`. and default value is
                            `off`.
                          type: string
                      required:
                      - httpsSwitch
                      type: object
                    type: array
                  hwPrivateAccess:
                    description: Access authentication for OBS origin.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: Access ID.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        bucket:
                          description: Bucket.
                          type: string
                        secretKeySecretRef:
                          description: Key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  ipFilter:
                    description: Specify Ip filter configurations.
                    items:
                      properties:
                        filterRules:
                          description: Ip filter rules, This feature is only available
                            to selected beta customers.
                          items:
                            properties:
                              filterType:
                                description: Ip filter `blacklist`/`whitelist` type
                                  of filter rules.
                                type: string
                              filters:
                                description: Ip filter rule list, supports IPs in
                                  X.X.X.X format, or /8, /16, /24 format IP ranges.
                                  Up to 50 allowlists or blocklists can be entered.
                                items:
                                  type: string
                                type: array
                              rulePaths:
                                description: 'Content list for each `rule_type`: `*`
                                  for `all`, file ext like `jpg` for `file`, `/dir/like/`
                                  for `directory` and `/path/index.html` for `path`.'
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: 'Ip filter rule type of filter rules,
                                  available: `all`, `file`, `directory`, `path`.'
                                type: string
                            required:
                            - filterType
                            - filters
                            - rulePaths
                            - ruleType
                            type: object
                          type: array
                        filterType:
                          description: IP `blacklist`/`whitelist` type.
                          type: string
                        filters:
                          description: Ip filter list, Supports IPs in X.X.X.X format,
                            or /8, /16, /24 format IP ranges. Up to 50 allowlists
                            or blocklists can be entered.
                          items:
                            type: string
                          type: array
                        returnCode:
                          description: 'Return code, available values: 400-499.'
                          type: number
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  ipFreqLimit:
                    description: Specify Ip frequency limit configurations.
                    items:
                      properties:
                        qps:
                          description: Sets the limited number of requests per second,
                            514 will be returned for requests that exceed the limit.
                          type: number
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  ipv6AccessSwitch:
                    description: ipv6 access configuration switch. Only available
                      when area set to `mainland`. Valid values are `on` and `off`.
                      Default value is `off`.
                    type: string
                  maxAge:
                    description: Browser cache configuration. (This feature is in
                      beta and not generally available yet).
                    items:
                      properties:
                        maxAgeRules:
                          description: List of Max Age rule configuration.
                          items:
                            properties:
                              followOrigin:
                                description: 'Whether to follow origin, values: `on`/`off`,
                                  if set to `on`, the `max_age_time` will be ignored.'
                                type: string
                              maxAgeContents:
                                description: 'List of rule paths for each `max_age_type`:
                                  `*` for `all`, file ext like `jpg` for `file`, `/dir/like/`
                                  for `directory` and `/path/index.html` for `path`.'
                                items:
                                  type: string
                                type: array
                              maxAgeTime:
                                description: Max Age time in seconds, this can set
                                  to `0` that stands for no cache.
                                type: number
                              maxAgeType:
                                description: 'The following types are supported: `all`:
                                  all documents take effect, `file`: the specified
                                  file suffix takes effect, `directory`: the specified
                                  path takes effect, `path`: specify the absolute
                                  path to take effect, `index`: home page, `default`:
                                  effective when the source site has no max-age.'
                                type: string
                            required:
                            - maxAgeContents
                            - maxAgeTime
                            - maxAgeType
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  offlineCacheSwitch:
                    description: 'Offline cache switch, available values: `on`, `off`
                      (default).'
                    type: string
                  origin:
                    description: Origin server configuration. It's a list and consist
                      of at most one item.
                    items:
                      properties:
                        backupOriginList:
                          description: Backup origin server list. Valid values can
                            be ip or domain name. When modifying the backup origin
                            server, you need to enter the corresponding `backup_origin_type`.
                          items:
                            type: string
                          type: array
                        backupOriginType:
                          description: 'Backup origin server type, which supports
                            the following types: `domain`: domain name type, `ip`:
                            IP list used as origin server.'
                          type: string
                        backupServerName:
                          description: Host header used when accessing the backup
                            origin server. If left empty, the ServerName of master
                            origin server will be used by default.
                          type: string
                        cosPrivateAccess:
                          description: When OriginType is COS, you can specify if
                            access to private buckets is allowed. Valid values are
                            `on` and `off`. and default value is `off`.
                          type: string
                        originList:
                          description: Master origin server list. Valid values can
                            be ip or domain name. When modifying the origin server,
                            you need to enter the corresponding `origin_type`.
                          items:
                            type: string
                          type: array
                        originPullProtocol:
                          description: 'Origin-pull protocol configuration. `http`:
                            forced HTTP origin-pull, `follow`: protocol follow origin-pull,
                            `https`: forced HTTPS origin-pull. This only supports
                            origin server port 443 for origin-pull.'
                          type: string
                        originType:
                          description: 'Master origin server type. The following types
                            are supported: `domain`: domain name type, `cos`: COS
                            origin, `ip`: IP list used as origin server, `ipv6`: origin
                            server list is a single IPv6 address, `ip_ipv6`: origin
                            server list is multiple IPv4 addresses and an IPv6 address.'
                          type: string
                        serverName:
                          description: Host header used when accessing the master
                            origin server. If left empty, the acceleration domain
                            name will be used by default.
                          type: string
                      required:
                      - originList
                      - originType
                      type: object
                    type: array
                  originPullOptimization:
                    description: Cross-border linkage optimization configuration.
                      (This feature is in beta and not generally available yet).
                    items:
                      properties:
                        optimizationType:
                          description: 'Optimization type, values: `OVToCN` - Overseas
                            to CN, `CNToOV` CN to Overseas.'
                          type: string
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  originPullTimeout:
                    description: Cross-border linkage optimization configuration.
                    items:
                      properties:
                        connectTimeout:
                          description: 'The origin-pull connection timeout (in seconds).
                            Valid range: 5-60.'
                          type: number
                        receiveTimeout:
                          description: 'The origin-pull receipt timeout (in seconds).
                            Valid range: 10-60.'
                          type: number
                      required:
                      - connectTimeout
                      - receiveTimeout
                      type: object
                    type: array
                  ossPrivateAccess:
                    description: Access authentication for OSS origin.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: Access ID.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        bucket:
                          description: Bucket.
                          type: string
                        region:
                          description: Region.
                          type: string
                        secretKeySecretRef:
                          description: Key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  postMaxSize:
                    description: Maximum post size configuration.
                    items:
                      properties:
                        maxSize:
                          description: Maximum size in MB, value range is `[1, 200]`.
                          type: number
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  projectId:
                    description: The project CDN belongs to, default to 0.
                    type: number
                  qnPrivateAccess:
                    description: Access authentication for OBS origin.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: Access ID.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        secretKeySecretRef:
                          description: Key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  quicSwitch:
                    description: 'QUIC switch, available values: `on`, `off` (default).'
                    type: string
                  rangeOriginSwitch:
                    description: Sharding back to source configuration switch. Valid
                      values are `on` and `off`. Default value is `on`.
                    type: string
                  referer:
                    description: Referer configuration.
                    items:
                      properties:
                        refererRules:
                          description: List of referer rules.
                          items:
                            properties:
                              allowEmpty:
                                description: Whether to allow emptpy.
                                type: boolean
                              refererType:
                                description: Referer type.
                                type: string
                              referers:
                                description: Referer list.
                                items:
                                  type: string
                                type: array
                              rulePaths:
                                description: Referer rule path list.
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: Referer rule type.
                                type: string
                            required:
                            - allowEmpty
                            - refererType
                            - referers
                            - rulePaths
                            - ruleType
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  requestHeader:
                    description: Request header configuration. It's a list and consist
                      of at most one item.
                    items:
                      properties:
                        headerRules:
                          description: Custom request header configuration rules.
                          items:
                            properties:
                              headerMode:
                                description: 'Http header setting method. The following
                                  types are supported: `add`: add a head, if a head
                                  already exists, there will be a duplicate head,
                                  `del`: delete the head.'
                                type: string
                              headerName:
                                description: Http header name.
                                type: string
                              headerValue:
                                description: Http header value, optional when Mode
                                  is `del`, Required when Mode is `add`/`set`.
                                type: string
                              rulePaths:
                                description: 'Matching content under the corresponding
                                  type of CacheType: `all`: fill *, `file`: fill in
                                  the suffix name, such as jpg, txt, `directory`:
                                  fill in the path, such as /xxx/test, `path`: fill
                                  in the absolute path, such as /xxx/test.html.'
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: 'Rule type. The following types are supported:
                                  `all`: all documents take effect, `file`: the specified
                                  file suffix takes effect, `directory`: the specified
                                  path takes effect, `path`: specify the absolute
                                  path to take effect.'
                                type: string
                            required:
                            - headerMode
                            - headerName
                            - headerValue
                            - rulePaths
                            - ruleType
                            type: object
                          type: array
                        switch:
                          description: Custom request header configuration switch.
                            Valid values are `on` and `off`. and default value is
                            `off`.
                          type: string
                      type: object
                    type: array
                  responseHeader:
                    description: Response header configurations.
                    items:
                      properties:
                        headerRules:
                          description: List of response header rule.
                          items:
                            properties:
                              headerMode:
                                description: Response header mode.
                                type: string
                              headerName:
                                description: response header name of rule.
                                type: string
                              headerValue:
                                description: response header value of rule.
                                type: string
                              rulePaths:
                                description: response rule paths of rule.
                                items:
                                  type: string
                                type: array
                              ruleType:
                                description: response rule type of rule.
                                type: string
                            required:
                            - headerMode
                            - headerName
                            - headerValue
                            - rulePaths
                            - ruleType
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  responseHeaderCacheSwitch:
                    description: 'Response header cache switch, available values:
                      `on`, `off` (default).'
                    type: string
                  ruleCache:
                    description: Advanced path cache configuration.
                    items:
                      properties:
                        cacheTime:
                          description: Cache expiration time setting, the unit is
                            second, the maximum can be set to 365 days.
                          type: number
                        compareMaxAge:
                          description: Advanced cache expiration configuration. When
                            it is turned on, it will compare the max-age value returned
                            by the origin site with the cache expiration time set
                            in CacheRules, and take the minimum value to cache at
                            the node. Valid values are `on` and `off`. Default value
                            is `off`.
                          type: string
                        followOriginSwitch:
                          description: Follow the source station configuration switch.
                            Valid values are `on` and `off`.
                          type: string
                        ignoreCacheControl:
                          description: Force caching. After opening, the no-store
                            and no-cache resources returned by the origin site will
                            also be cached in accordance with the CacheRules rules.
                            Valid values are `on` and `off`. Default value is `off`.
                          type: string
                        ignoreSetCookie:
                          description: Ignore the Set-Cookie header of the origin
                            site. Valid values are `on` and `off`. Default value is
                            `off`. This parameter is for white-list customer.
                          type: string
                        noCacheSwitch:
                          description: Cache configuration switch. Valid values are
                            `on` and `off`.
                          type: string
                        reValidate:
                          description: Always check back to origin. Valid values are
                            `on` and `off`. Default value is `off`.
                          type: string
                        rulePaths:
                          description: 'Matching content under the corresponding type
                            of CacheType: `all`: fill *, `file`: fill in the suffix
                            name, such as jpg, txt, `directory`: fill in the path,
                            such as /xxx/test, `path`: fill in the absolute path,
                            such as /xxx/test.html, `index`: fill /, `default`: Fill
                            `no max-age`.'
                          items:
                            type: string
                          type: array
                        ruleType:
                          description: 'Rule type. The following types are supported:
                            `all`: all documents take effect, `file`: the specified
                            file suffix takes effect, `directory`: the specified path
                            takes effect, `path`: specify the absolute path to take
                            effect, `index`: home page, `default`: effective when
                            the source site has no max-age.'
                          type: string
                        switch:
                          description: Cache configuration switch. Valid values are
                            `on` and `off`.
                          type: string
                      required:
                      - cacheTime
                      type: object
                    type: array
                  seoSwitch:
                    description: 'SEO switch, available values: `on`, `off` (default).'
                    type: string
                  serviceType:
                    description: 'Acceleration domain name service type. `web`: static
                      acceleration, `download`: download acceleration, `media`: streaming
                      media VOD acceleration.'
                    type: string
                  specificConfigMainland:
                    description: 'Specific configuration for mainland, NOTE: Both
                      specifying full schema or using it is superfluous, please use
                      cloud api parameters json passthroughs, check the [Data Types](https://www.tencentcloud.com/document/api/228/31739#MainlandConfig)
                      for more details.'
                    type: string
                  specificConfigOverseas:
                    description: 'Specific configuration for oversea, NOTE: Both specifying
                      full schema or using it is superfluous, please use cloud api
                      parameters json passthroughs, check the [Data Types](https://www.tencentcloud.com/document/api/228/31739#OverseaConfig)
                      for more details.'
                    type: string
                  statusCodeCache:
                    description: Status code cache configurations.
                    items:
                      properties:
                        cacheRules:
                          description: List of cache rule.
                          items:
                            properties:
                              cacheTime:
                                description: Status code cache expiration time (in
                                  seconds).
                                type: number
                              statusCode:
                                description: 'Code of status cache. available values:
                                  `403`, `404`.'
                                type: string
                            required:
                            - cacheTime
                            - statusCode
                            type: object
                          type: array
                        switch:
                          description: 'Configuration switch, available values: `on`,
                            `off` (default).'
                          type: string
                      required:
                      - switch
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: Tags of cdn domain.
                    type: object
                  videoSeekSwitch:
                    description: 'Video seek switch, available values: `on`, `off`
                      (default).'
                    type: string
                required:
                - domain
                - origin
                - serviceType
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DomainStatus defines the observed state of Domain.
            properties:
              atProvider:
                properties:
                  bandWidthAlert:
                    items:
                      properties:
                        lastTriggerTime:
                          type: string
                        lastTriggerTimeOverseas:
                          type: string
                      type: object
                    type: array
                  cname:
                    type: string
                  createTime:
                    type: string
                  dryRunCreateResult:
                    type: string
                  dryRunUpdateResult:
                    type: string
                  httpsConfig:
                    items:
                      properties:
                        clientCertificateConfig:
                          items:
                            properties:
                              certificateName:
                                type: string
                              deployTime:
                                type: string
                              expireTime:
                                type: string
                            type: object
                          type: array
                        serverCertificateConfig:
                          items:
                            properties:
                              certificateName:
                                type: string
                              deployTime:
                                type: string
                              expireTime:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    type: string
                  status:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
